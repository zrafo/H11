Despues de instalar Node js
Powershell:

get-ExecutionPolicy -List

Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

registramos una app en firebase 

abrir carpeta de la web

nuevo terminal

npm install firebase

luego de eso antes que cierre body pegar el sdk (copiar dle firebase)

en el terminal:
instalar: npm install -g firebase-tools

iniciar sesion:
firebase login 

firebase init

Y

espacio en:
Hosting: Configure files for Firebase Hosting and (optionally) set up GitHub Action deploys
enter

use an existing project

elegir el proyecto (enter)

What do you want to use as your public directory? public (enter)
Configure as a single-page app (rewrite all urls to /index.html)? (y/N) Y (enter)
Set up automatic builds and deploys with GitHub? (y/N) N (enter)

firebase deploy

mover todos los archivos del proyecto a la carpeta public (reemplazar index)

ejecutar nuevamente firebase deploy




https://skillshop.exceedlms.com/student/collection/799479-web-development-i?locale=es



